# macOS metadata
.DS_Store
**/.DS_Store

# Windows metadata
Thumbs.db
ehthumbs.db

# Editor backups
*~
*.swp
*.swo
.idea/
.vscode/

# Private — view locally only, NEVER deploy
# (contains sensitive birth date data — user keeps for personal reference)
brothers-age.html
